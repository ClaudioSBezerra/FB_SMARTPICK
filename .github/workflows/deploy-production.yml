name: Deploy to Production (Coolify + Hostinger)

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3.10.0

      - name: Log in to Container Registry
        uses: docker/login-action@v3.4.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5.7.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6.15.0
        with:
          context: .
          file: ./Dockerfile.production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  deploy-coolify:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: needs.build-and-push.result == 'success'

    steps:
      - name: Trigger Coolify redeploy
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "http://76.13.171.196:8000/api/v1/deploy?uuid=u4k84840408sc4wgogcw4c04&force=false")
          echo "Coolify HTTP status: $STATUS"

  notify:
    needs: [build-and-push, deploy-coolify]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Deploy status
        run: |
          echo "Build:  ${{ needs.build-and-push.result }}"
          echo "Deploy: ${{ needs.deploy-coolify.result }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Data:   $(date)"
